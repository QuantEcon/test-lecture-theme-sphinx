---
jupytext:
  text_representation:
    extension: .md
    format_name: myst
kernelspec:
  display_name: Python 3
  language: python
  name: python3
---

# Theme Parity Test Lectures

This site is a **test corpus**, not a lecture series. Its pages exercise every
MyST construct the QuantEcon Python lecture series use -- measured across the
eight lecture repositories on 2026-09-16 -- so that the same content can be
built with Jupyter Book 1 (this site, on `quantecon-book-theme`) and with the
QuantEcon mystmd fork (its sibling, `test-lecture-theme-mystmd`) and the two
renderings compared feature by feature.

Each page covers one family of constructs and says, in a comment at the top,
which lectures the examples were taken from.

```{tableofcontents}
```
